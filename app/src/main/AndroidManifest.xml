<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RenderEffectX">
        <!-- Landscape demo scene — the launcher; recorded for the README/social material. -->
        <activity
            android:name=".DemoPagerActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="userLandscape"
            android:theme="@style/Theme.RenderEffectX">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kept for perf measurement: `am start -n …/.MainActivity -e scene static`. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.RenderEffectX"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>